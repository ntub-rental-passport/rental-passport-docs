flowchart TB
    %% ═════════════════════════════════════════════════════════════
    %% 圖 7-3-1　RentMate 系統元件圖
    %% 風格：技術元件依賴圖（比照學長 IS Capt. 範例）
    %% ‧ 單色、無協定標籤、雙向箭頭、外部服務不分組
    %% ═════════════════════════════════════════════════════════════

    FE["📦 Frontend<br/>(Vue 3, TypeScript, TailwindCSS, PWA)"]
    BE["📦 Backend<br/>(Python, FastAPI)"]
    AI["📦 AI Module<br/>(Embedding Model, RAG)"]
    DB["📦 MySQL<br/>(SQLAlchemy, Alembic)"]
    Chroma["📦 ChromaDB<br/>(Vector Database)"]
    Vision["📦 Google Cloud Vision<br/>(OCR API)"]
    Claude["📦 Anthropic Claude API<br/>(Sonnet/Haiku 4.5)"]
    OAuth["📦 Google OAuth API"]
    Push["📦 Web Push Service"]
    GovData["📦 政府開放資料"]

    %% ── 主要依賴關係 ────────────────────────────────────────────
    FE --> BE
    BE --> DB
    BE --> Chroma
    BE <--> AI
    AI --> Vision
    AI --> Claude
    BE --> OAuth
    BE <--> Push
    BE --> GovData
    Push -.-> FE

    %% ── 顏色：全部單色（淺灰白），跟學長範例一致 ───────────────
    classDef comp fill:#F3F4F6,stroke:#374151,stroke-width:1.5px,color:#111827
    class FE,BE,AI,DB,Chroma,Vision,Claude,OAuth,Push,GovData comp